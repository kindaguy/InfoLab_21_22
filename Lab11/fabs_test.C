#include <iostream>
#include <cmath>

using namespace std;

int main(){

   float a = 0.04;
   float b;
   double c;
   cout << endl << a << endl;
   cout << endl << fabs(a) << endl;
   b=fabs(a);
   cout << endl << fabs(b) << endl;
   c=fabs(a);
   cout << endl << fabs(c) << endl;

}